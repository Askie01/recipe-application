spring:
  datasource:
    url: jdbc:h2:mem:test-database
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: always
      schema-locations: "classpath:schema/*.sql"
      data-locations: "classpath:data/*.sql"